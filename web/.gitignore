# dependencies
node_modules

# build
dist
dist-ssr
.vercel

# env
.env
*.local

# Native *build* output is ignored inside android/.gitignore and
# ios/.gitignore. Do not ignore those folders — the store shells belong
# in the repo so Android, iOS and desktop can change independently.

# Expo
.expo

# debug
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

# IDE
.vscode/*
!.vscode/extensions.json
.idea

# macOS
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
