# Desktop (Windows / Microsoft Store)

Not generated yet. This folder is the PC installer / Microsoft Store shell.

The game itself stays in `../src`. Windows players can use the website today:

https://chess-of-warlords.vercel.app/

A Store package (Electron or Tauri) will wrap that same build. Do not put hall
/ chess / HUD changes here — those belong in `../src`.
